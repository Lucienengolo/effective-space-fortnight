# E-KYC App — Streamlit Cloud deployment
# Push this repo to GitHub, then deploy at https://share.streamlit.io

streamlit>=1.35.0
Pillow>=10.0.0

# Face verification — DeepFace (uses tf-keras on Streamlit Cloud)
deepface>=0.0.93
tf-keras>=2.16.0

# OCR — Hugging Face TrOCR
transformers>=4.40.0
torch>=2.0.0

# OCR — Tesseract fallback
# Streamlit Cloud has the tesseract binary pre-installed, only need the wrapper
pytesseract>=0.3.10
